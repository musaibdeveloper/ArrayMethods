// keys()

let names = ["musaib", "sakina", "shoail", "hanzala"]

// The keys() method returns a new Array Iterator object that 
// contains the keys for each element in the array.


 let result = names.keys();

//  HERE  a vaiable is define to store the method

 for (let names of result){
    console.log(names);
 }

//  for loop is used to iterator the array elements.