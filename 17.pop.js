// /Array method pop. 

// : This method removes the last element
// from an array and returns that element.

// It removes the last element only

let arr = ["Apple", "Banana", "Mango", "cherry"]

let result = arr.pop();

console.log(arr);