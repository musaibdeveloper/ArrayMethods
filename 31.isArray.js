// Is Array.

// isArray(): This method determines whether the
// passed value is an array or not.

// The output is in boolean Value.

let arr = ["Apple", "Mango", "Grapes"]

console.log(Array.isArray(arr)); // true 

let numbers = 5465465;

console.log(Array.isArray(numbers)); // false.